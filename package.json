{
  "name": "docker-sandbox-express",
  "version": "1.0.0",
  "description": "Simple Express HTTP server for testing Docker image execution on Vercel Functions and Vercel Sandboxes",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
